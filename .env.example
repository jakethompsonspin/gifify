# Optional: limit maximum GIF size (MB) and default processing settings
MAX_GIF_MB=50
DEFAULT_FPS=12
DEFAULT_WIDTH=480
PORT=8080
ALLOWED_ORIGINS=*
# Path overrides if yt-dlp/ffmpeg aren't in PATH
YTDLP_PATH=yt-dlp
FFMPEG_PATH=ffmpeg
FFPROBE_PATH=ffprobe
